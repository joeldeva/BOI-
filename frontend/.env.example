# FraudShield DeceptiScope - public, non-secret build configuration.
# Leave the API URL empty for same-origin Vite/nginx proxying (recommended).
VITE_API_BASE_URL=
VITE_APP_TITLE=FraudShield DeceptiScope
VITE_MAX_APK_MB=75
